//questa classe contiene un modello per le domande, significa che ogni domanda conterrà domande e valori vero o falso
class Question {
  String questionText = '';
  bool questionAnswer = false;

  Question(this.questionText, this.questionAnswer);
}
